PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=esprittech
JWT_SECRET=change_this_secret_in_production

# Sauvegardes automatiques — nécessite le client mysqldump installé et dans le PATH.
# Si mysqldump n'est pas dans le PATH, indiquez son chemin complet ici.
# ex. Windows: C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe
# MYSQLDUMP_PATH=

# Power BI — clé requise en paramètre ?key=... sur /api/powerbi/*
# (Power BI Desktop ne peut pas faire le login JWT normal de l'app).
POWERBI_API_KEY=arp2026secret
